calculator-app/
│
├── .github/
│   └── workflows/
│       └── docker-build.yml       # GitHub Actions workflow for Docker build & push
│
├── docker/                        # Docker-related files
│   ├── Dockerfile                  # App image definition
│   └── default.conf                # Nginx / server config
│
├── k8s/                            # Kubernetes manifests
│   ├── deployment.yaml
│   ├── service.yaml
│   └── ingress.yaml                # Optional ingress configuration
│
├── jenkins/                        # Jenkins CI/CD pipeline
│   └── Jenkinsfile
│
├── frontend/                       # Static front-end files (renamed from "template" for clarity)
│   ├── index.html
│   ├── style.css
│   └── script.js
│
├── .dockerignore
├── .gitignore
└── README.md

#Use docker build command:
docker build -t aishawon.calculator-app:latest -f docker/Dockerfile .

#Short Project Structure
calculator-app/
├── templates/                 # Static frontend files
│   ├── index.html
│   ├── style.css
│   └── script.js
│
├── docker/                    # All Docker-related files
│   ├── Dockerfile
│   └── default.conf
│
├── docker-compose.yml         # Run app easily (optional but recommended)
├── .dockerignore              # Ignore unnecessary files for Docker build
├── .gitignore                 # Git ignore rules
├── README.md                  # Project documentation